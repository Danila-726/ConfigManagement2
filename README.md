```markdown
# ConfigManagement2 - Инструмент анализа зависимостей пакетов Alpine Linux

Утилита для визуализации и анализа графа зависимостей пакетов Alpine Linux.

## 📁 Структура проекта

```
CONFIGMANAGEMENT2/
├── app/                          # Основной код приложения
│   ├── __pycache__/
│   ├── apk_parser.py            # Парсер APK пакетов
│   ├── cli.py                   # Обработка командной строки
│   ├── dependency_graph.py      # Построение графа зависимостей
│   ├── exceptions.py            # Кастомные исключения
│   ├── main.py                  # Главный модуль
│   └── repository.py            # Менеджер репозиториев
├── content/                     # Тестовые данные
│   ├── test_repo_complex.txt
│   ├── test_repo.json
│   └── test_repository.txt
├── .gitignore
├── LICENSE
└── README.md
```

## 🚀 Быстрый старт

### Установка зависимостей
```bash
# Не требуются внешние зависимости (используется только стандартная библиотека Python)
```

### Запуск приложения
```bash
python app/main.py --package <имя_пакета> --repo <репозиторий> --output <выходной_файл>
```

## 📋 Основные возможности

### 🔍 Анализ зависимостей
- **Прямые зависимости** - непосредственные зависимости пакета
- **Транзитивные зависимости** - все зависимости (прямые и косвенные)
- **Обратные зависимости** - пакеты, зависящие от целевого
- **Обнаружение циклов** - выявление циклических зависимостей

### 📊 Визуализация
- **ASCII-дерево** - текстовое представление дерева зависимостей
- **Граф зависимостей** - полный граф всех связей между пакетами

### 🔧 Режимы работы
- **Тестовый режим** (`--test-repo-mode`) - работа с локальными файлами репозитория
- **Продакшн режим** - загрузка репозитория по URL
- **Режим обратных зависимостей** (`--reverse`) - анализ кто зависит от пакета

## 💻 Использование

### Базовый пример
```bash
python app/main.py --package busybox --repo content/test_repository.txt --output graph.png --test-repo-mode
```

### Расширенный пример
```bash
python app/main.py \
  --package alpine-baselayout \
  --repo https://dl-cdn.alpinelinux.org/alpine/v3.18/main/x86_64/APKINDEX.tar.gz \
  --output dependencies.svg \
  --ascii-tree \
  --reverse
```

## 🛠️ Модули системы

### `apk_parser.py`
- Парсинг APKINDEX формата Alpine Linux
- Извлечение зависимостей из секций пакетов
- Очистка имен пакетов от версий и префиксов

### `dependency_graph.py`
- Построение полного графа зависимостей (DFS без рекурсии)
- Обнаружение циклических зависимостей
- Анализ прямых и обратных зависимостей

### `repository.py`
- Управление локальными и удаленными репозиториями
- Автоматическая загрузка и кэширование
- Очистка временных файлов

### `cli.py`
- Валидация входных параметров
- Парсинг аргументов командной строки
- Проверка корректности путей и URL

